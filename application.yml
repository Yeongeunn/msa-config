spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://eunna-db.ch6w0ggq29jc.us-west-2.rds.amazonaws.com:3306/inspire?useSSL=true&trustServerCertificate=true
    username: admin
    password: 123456789
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true

  data:
    redis:
      host: redis-service
      port: 6379
      timeout: 6000ms

  rabbitmq:
    host: rabbitmq-service
    port: 5672
    username: guest
    password: guest

  cloud:
    bus:
      enabled: true
